function main (numOne, numTwo) {
    
    var numOne; 
    var numTwo;
    var difference; 

    difference = numOne - numTwo; 

    return difference;

    
}
console.log(main(numOne, numTwo)); 
console.log(main(numOne, numTwo));
console.log(main(numOne, numTwo));

module.exports = main;